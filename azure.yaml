# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-ai-tips
metadata:
  template: azure-ai-tips

services:
  app:
    project: ./app
    language: python
    host: appservice

infra:
  provider: bicep
  path: ./speech/infra
